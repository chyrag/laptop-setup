# Powerlevel10k prompt configuration.
# Run `p10k configure` in a zsh session to generate your personal config,
# then copy the resulting ~/.p10k.zsh here.
#
# This placeholder prevents errors on first bootstrap — p10k will use its
# default prompt until you run the configurator.
